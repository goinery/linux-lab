# ChatRoom 当前 UI 设计说明

## 1. 设计目标

当前版本 UI 以以下目标为核心：

- 单窗口完成全流程：启动配置、服务端信息、客户端登录注册、聊天页。
- 现代化视觉：圆角卡片、轻量阴影感、统一留白、清晰层次。
- 主题可切换：支持浅色/深色两套风格。
- 交互可达性：关键动作集中在主路径（下一步、返回、登录/注册、主题、全屏）。
- 状态可见：连接失败、参数错误、认证结果通过页面内状态区提示，不依赖弹窗。

## 2. 布局结构（单窗口架构）

主界面使用一个 `QMainWindow` 作为容器，分为两层：

1. 顶部操作条（Top Bar）
- 左侧：应用标题（统一控制台）
- 右侧：`切换主题`、`全屏`

2. 中央内容区（Stacked Pages）
- 使用 `QStackedWidget` 管理多页面流转
- 页面包括：
  - 启动配置页
  - 服务端监控页
  - 客户端认证页（登录/注册）
  - 聊天页（登录成功后接入）

## 3. 窗口行为

- 启动后窗口自动居中显示。
- 启动尺寸为可用屏幕宽高的 1/2。
- 支持全屏切换。
- 最小尺寸约束，保证表单和卡片在中小屏下可用。

## 4. 页面与流程

### 4.1 启动配置页

功能：选择角色并输入连接参数。

- 角色选择：
  - 作为服务端
  - 作为客户端
- 动态表单：
  - 服务端模式：输入监听端口
  - 客户端模式：输入服务端地址 + 端口
- 行为：点击 `下一步`
  - 服务端：启动服务并进入服务端监控页
  - 客户端：尝试连接并进入客户端认证页
- 错误提示：参数非法或连接失败时，显示在页面状态区

### 4.2 服务端监控页

功能：展示服务端运行信息。

- 显示本机可用 IPv4 地址
- 显示服务端监听端口
- 显示在线人数与在线用户列表
- 提供返回启动页入口

### 4.3 客户端认证页

功能：客户端用户身份认证。

- 模式切换：登录 / 注册（同页切换，不弹窗）
- 登录字段：用户名、密码
- 注册字段：用户名、密码、确认密码
- 认证结果提示：页面状态区显示（成功/失败）
- 登录成功：切换到聊天页（仍在同一主窗口）

### 4.4 聊天页

功能：承载现有聊天 UI 及消息交互能力。

- 复用既有聊天主界面模块
- 显示会话列表、消息区域、输入区
- 保留未读计数与消息滚动等已实现交互

## 5. 视觉规范

### 5.1 风格关键词

- 清新、简洁、圆角、低负担

### 5.2 组件样式

- 卡片容器：大圆角（约 18px）、明显分区
- 按钮：统一圆角（约 12px）
- 输入框/列表：统一圆角边框、轻量边框色
- 主操作按钮：高对比强调色
- 状态文案：固定状态区，避免界面跳动

### 5.3 主题系统

- 浅色主题：高亮背景、深色文字、清爽分层
- 深色主题：深背景、浅文字、控制高对比不过曝
- 主题切换作用于全局样式表

## 6. 交互细节

- 无阻塞提示优先：错误和状态尽量在页面内反馈
- 表单前置校验：端口范围、用户名密码长度、两次密码一致
- 导航闭环：每个步骤可返回上一步
- 单任务主路径清晰：
  - 选角色 -> 配参数 -> 下一步 -> 进入对应工作页

## 7. 当前实现边界

- 客户端连接检查在流程中完成，但仍依赖网络状态实时反馈。
- 聊天页目前为“接入现有模块”方式，视觉统一度后续可继续提升（例如顶部条与聊天页风格进一步融合）。
- 服务端监控页当前聚焦基础运行信息，尚未加入更多运维指标（如消息吞吐、连接时长等）。

## 8. 后续优化建议

- 统一设计令牌：抽离颜色、圆角、间距为主题变量。
- 动效增强：页面切换加入轻量淡入/滑入，提升“流程感”。
- 响应式细化：针对低分辨率窗口优化字段排布与最小高度。
- 监控增强：服务端页增加连接日志时间线与关键事件提示。
- 可访问性：补充键盘焦点顺序、高对比度模式与字号级别选项。
